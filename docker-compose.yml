# db:
#   image: postgres:9.4.1
#   ports:
#     - "5432:5432"

web:
  build: ./simple_rails_app
  command: bin/rails server --port 3000 --binding 0.0.0.0
  ports:
    - "3000:3000"
  # links:
  #   - db
  volumes:
    - ./simple_rails_app:/myapp